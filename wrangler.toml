# Cloudflare Pages 配置文件
# 部署命令: npm run build
# 输出目录: dist/
# 构建命令: npm install && npm run crawl && npm run build

[build]
command = "npm install && npm run crawl && npm run build"
publish = "dist"

[build.environment]
NODE_VERSION = "20"
PYTHON_VERSION = "3.10"
